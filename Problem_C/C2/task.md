**Model:** GPT-4o

You inherited a data processing function from a colleague. It takes 500,000 sales records and returns total revenue per category. It's slow. Your job: make it faster.

Optimize the `get_revenue_by_category` function in `your_solution.py`. Only modify `your_solution.py` — do not touch any other files.

**How to run:**
- Your solution: `python your_solution.py`
- Compare with optimal: `python compare.py`
