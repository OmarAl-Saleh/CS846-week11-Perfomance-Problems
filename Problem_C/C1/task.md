**Model:** GPT-4o

Given 1 million random integers between 1 and 100,000, find the difference between the smallest and largest numbers whose digits sum to 30. Optimize the solution for performance.

Implement the `find_difference(numbers)` function in `your_solution.py`. Only modify `your_solution.py` — do not touch any other files.

**How to run:**
- Your solution: `python your_solution.py`
- Compare with optimal: `python compare.py`
